<h1><%=@cocktail.name %></h1>

<ul>
  <% @doses.each do | dose| %>
  <li><%= dose.description %> <%= dose.ingredient.name %> <%= link_to dose_path(dose), method: :delete, data: { confirm: "Delete?" } do %>
      <i class="far fa-trash-alt"></i>
    <% end %> </li>
  <% end %>
</ul>



<%= link_to "Add Ingredient", new_cocktail_dose_path(@cocktail) %>


